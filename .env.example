# Supabase Configuration
# Get these values from your Supabase project settings (Settings > API)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# SECURITY WARNING: NEVER add VITE_SUPABASE_SERVICE_ROLE_KEY here!
# The service_role key bypasses Row Level Security and should ONLY be used
# in server-side code (backend APIs, edge functions, etc.).
# 
# Key Differences:
# - anon/public key: Safe for client-side, respects RLS policies
# - service_role key: NEVER expose to client, bypasses RLS, server-side only
#
# If you need server-side access, create a separate backend service that uses
# the service_role key securely, and call that service from your frontend.

# Backend API Base URL (optional, if you have a custom backend)
# VITE_API_BASE_URL=http://localhost:3000/api

